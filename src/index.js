import React from 'react';
import ReactDOM from 'react-dom';
import ListeCulture from './ListeCulture';

ReactDOM.render(
  <React.StrictMode>
    <ListeCulture />
  </React.StrictMode>,
  document.getElementById('root')
);